{
  "defaultAction": "SCMP_ACT_ALLOW",
  "architectures": [
    "SCMP_ARCH_X86_64",
    "SCMP_ARCH_X86",
    "SCMP_ARCH_X32"
  ],
  "syscalls": [
    {
      "names": [
        "pivot_root",
        "kexec_load",
        "kexec_file_load",
        "init_module",
        "finit_module",
        "delete_module",
        "iopl",
        "ioperm",
        "perf_event_open",
        "add_key",
        "request_key",
        "keyctl",
        "bpf"
      ],
      "action": "SCMP_ACT_ERRNO",
      "comment": "Block kernel-level syscalls that could be used for container escape or host compromise. mount/umount2 are intentionally omitted: fuse-overlayfs requires them, and the AppArmor profile restricts allowed mount types to fstype=fuse.* only."
    }
  ]
}
